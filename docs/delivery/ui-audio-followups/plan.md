# Delivery Plan

1. Correct the filter damping coefficient and add a standalone native stability
   check. Close after focused check, full gates, device tests, review, and commit.
2. Give Piano mode its allocated height and add a Compose height regression.
3. Constrain Workflow Guide children and add Compose bounds regressions.
4. Add cancellable press/release pad audition to Timeline chips and cover the
   touch lifecycle in the Timeline harness.
5. Delete the PadClip marker, run the Timeline UI regression and capture the
   device result.

Every phase is independently reviewable and reversible. No later-phase cleanup
is included early.
